# Living design docs

Working documents for features under design, in flight, or investigated and
concluded. Each doc carries its own `Status:` header — that header is the source
of truth; this table is the index. Files keep stable paths for their whole life
(verdict docs stay citable; see `plan-static-array-erasure.md`, deliberately
re-added after a mass deletion left dangling references).

| Document | Status | Topic |
|----------|--------|-------|
| [plan-structural-performance-opportunities.md](plan-structural-performance-opportunities.md) | RESEARCHED: planning only | Ranked performance audit: verified quadratic reverse AD, shared access descriptions for demand/adjoints/ownership, streaming across reductions, reuse across immutable dataset revisions, and extensions to separability and constrained-domain enumeration; prior-art boundaries and first measurement gates |
| [plan-structural-synthesis.md](plan-structural-synthesis.md) | SYNTHESIS 2026-09-06; items 1-3 landed on master, item 5 (halo cotangents, structural/02) built on feat/halo-cotangents | Verdicts of the six elaborations under structural/ (five GO, one DEFER-to-idiom), the implementation order that followed, and the fourteen surface/policy decisions held for the user |
| [plan-fortran-killer-2.md](plan-fortran-killer-2.md) | IN PROGRESS: 3 correctness items on master (2026-09-05); P0 effect summaries + `blade plan`, P1 destination passing gate 1, P2 units on cotangents on feat/fortran-killer-2 (2026-09-06); indexed RNG LANDED 2026-09-07 (`rand.<fam>_at`, Philox4x32-10, KAT-pinned); factor-once / manifests / restart wait on surface decisions | Second non-symmetry audit: shared optimization legality facts, consumer-directed storage, indexed RNG, unit-aware reverse AD and reusable numerical operators, executable input contracts, and restartable recurrences; includes two confirmed wrong-answer reproducers (both fixed) and corrections to the first audit's assumptions |
| [plan-compact-sym-folds.md](plan-compact-sym-folds.md) | EXPLORATORY (2026-08-18) | What `reduce` over triangular (SymIdx/AntisymIdx) storage means: the BL3999 refusal census, the rank dichotomy, and `emitCompactFold` as the one canonical primitive |
| [plan-deferred-combinators.md](plan-deferred-combinators.md) | EXPLORATORY (2026-08-18); first blood 5a7b119 | What it would take for the eager combinator set to compose lazily: eagerness as a typecheck decision, the deferred `reduce` that already exists behind five declines |
| [plan-match-statements.md](plan-match-statements.md) | §2 fixes + §4 P1/P2 + §3 P0 LANDED; §4 P3-P5 open | Match census (two grammars, four evaluators with four pattern sets) and bug ledger — a silent tuple-slot wrong answer, arm types now unified, guards typed Bool, the recursive match emitter (cons/struct/nested-tuple, plus name-erased struct fields that bound out-of-order patterns wrongly in BOTH lanes); the `while` guard on recursive arrays (P0 built: early exit + freeze + BL8010 budget abort; `converged_at` and window riding open); and matching on types via rank dispatch — const-match fold hoisted, abstract rank peeling landed across THREE synthesis sites, with the rank worklist, peeled-rank recursion typecheck and type-name-in-pattern refusal open. Consume-by-rank-induction is v1; define-by-type-induction is research |
| [plan-ad-combinators.md](plan-ad-combinators.md) | ACTIVE (F4 spec) | Per-combinator forward/reverse AD rules; the companion spec `plan-forward-mode-ad.md` §4 F4 deferred to |
| [plan-simplex-blocked-compute.md](plan-simplex-blocked-compute.md) | P0+P1 LANDED (rank 2); P1 GATE FAILED | Reusing Zarr SimplexBlocks math on the compute side: dense bricks + 2^-d residue. Correct and measured; S0 costs ~7% and wins nothing — S1/mirror modes untouched |
| [plan-unroll-and-jam.md](plan-unroll-and-jam.md) | SPECIFIED: nothing built | Jam the enclosing OUTPUT axis around an in-nest fold: R independent chains without changing any cell's summation order, so BITWISE and licence-free where the licensed fold split measures 1.00x. Expect 3.4-4.3x in Blade's real shape (not the prototype's 13.95x). Two agents disagreed on tile width; §1 settles it — R<=4 AND explicit contraction suppression, since gcc contracts at >=8 accumulators under Blade's shipping `-ffp-contract=fast` |
| [plan-rank-r-former.md](plan-rank-r-former.md) | DESIGN: nothing built | BLAS-class rank-r symmetric contraction (`sum_t A(t,i1)...A(t,ir)`) with NO decompaction. The KRS schedule uses the packed pool's affine last axis to reduce rank-r to a ragged rank-2 gemm, claiming 100% of r! in flops where BCSS got storage only; bricks and S1 are NOT needed, and the real bottleneck is a dependent FMA chain. P0 is a census that can kill it |
| [plan-toolchain-packaging.md](plan-toolchain-packaging.md) | LANDED (doctor/setup phases open) | deps.json pins, `blade doctor`, `blade setup`, toolchain.json |
| [plan-forward-mode-ad.md](plan-forward-mode-ad.md) | EXECUTED through F3 | `ad.jvp` forward mode, HVP, second order |
| [plan-static-array-erasure.md](plan-static-array-erasure.md) | VERDICT: REFUTED | Measured case against static-extent array erasure — do not build |
| [plan-llvm-backend.md](plan-llvm-backend.md) | IMPLEMENTED through M5, measured | `BLADE_LLVM` lane: emits, refuses whole-program, `blade test llvm` / `llvm-bench`. Codegen 4.5x faster than g++; runtime at parity — the fact-emission thesis stays refuted, R6 (toolchain) is what pays |
| [plan-mlir-backend.md](plan-mlir-backend.md) | PROPOSAL: unscheduled | If-we-did-it MLIR architecture sketch (cuda-tile target) |
| [plan-llvm-runtime-shapes.md](plan-llvm-runtime-shapes.md) | RAGGED LANDED; group_by open | LLVM lane emits ragged/grouped shapes natively (RaggedIdx, EnumIdx/group_by): static ragged first (fully compile-time), then operand-valued extents, then CSR group_by — pure `.ll` for annotated key regimes, shim hash only for dynamic discovery |
| [plan-graphs-trees.md](plan-graphs-trees.md) | DESIGN REFRESHED; P0 next | `TreeIdx<shape>` static trees: flat preorder storage on the ragged CSR shape, one path-domain slot (the SparseIdx escape hatch), derived dense axes. Graphs need NO new index type — `Trace`/`DAGIdx` deleted in the design refresh (`docs/features/graphs-trees.md`); walks are `let rec`, collapse is MonadPlus, acyclicity is a checked `where acyclic(g)` license |
| [plan-equivariant-nn-notebooks.md](plan-equivariant-nn-notebooks.md) | ALL BUILT; CONDENSED 2026-08-28 | Four shipping notebooks: NB1a Tetris deduction (chirality as a compile error), NB1b MD17-aspirin benchmark (full-stack training, live `plot.stream` loss, forces = −∂E/∂r transforming at 1e-15), NB3 matched-moment discriminator (rung staircase 100/50/157/198 hits, on prediction), NB4 COD crystals (Neumann whitening, exact bond-order classification on real structures). NB2 pair-cloud moment jets REMOVED in the condensing pass — decisive negative after the E1–E4 campaign; §3 keeps the full record. Plus the display-stream infra (sink, `emit_id`, `plot.stream`, notebook renderer); 71-entry gap census; #40/#44 fixed; idiom-audited |
| [plan-subset-split.md](plan-subset-split.md) | PLANNED: P0 sized, nothing built | `subset(A, d, lo..hi)` + `split` (a two-subset desugar) — the missing inverse of `join` (formalism.md:157, BL2001-unbound today). Revives the dormant `IRSubset` node; P1 makes it grad-admissible (scatter-at-offset adjoint via the existing accum lane), collapsing the notebooks' hand-enumerated weight-slicer literals. ~20 mirrored join-twin arms, zero fsproj churn |
| [plan-cosmology-nongaussianity.md](plan-cosmology-nongaussianity.md) | RESEARCHED: nothing built | f_NL bispectrum/trispectrum as a lighthouse domain: two flat-sky notebooks (`ppl.cumulants(F,k)` IS the binned polyspectrum estimator, arity = order; BL3999 refusal = the Δ degeneracy factor), plus the compiler asks it surfaces — a CONFIRMED `comm`×`conj` silent-wrongness bug (probe in §5.0, chip raised), cosmology as the demand-driver for the parked compact-fold decision, and the constrained-index C2 layer's first customer |
| [structural/05-structured-operators.md](structural/05-structured-operators.md) | STEP 1 BUILT 2026-09-07: `gram_apply(A, B, x)` = A·(Bᴴx), the Gram action without the Gram matrix -- node, twins, transposed-gemv route, AD rules (adjoint action is a `gram_apply`); open: the operator VALUE form, factorization values (co-design with fortran-killer-2 §6.2), separability, the D7 advisory | Why a rewrite of `gram(A, A)`-applied is wrong for Blade (bits, observability, the advise-never-rewrite doctrine, fragility) and why an explicit applied-action node is the first step; the decision list D1-D7 |
| [structural/07-segmented-domains.md](structural/07-segmented-domains.md) | BUILT 2026-09-06 (feat/segmented-domains): type, `segments`/`files`/tile grouping, `ungroup`, zarr inheritance, streamed fold / group_by / elementwise / stencil-with-ghosts at rank 1 and 2 recorded for `blade plan`; open: nested two-level shape, 2-D mosaic declaration, streaming cost model | Chunks and files as two unrelated structural `group_by` levels of one axis (`Chunked<I, [[s, f], ...]>`, files first, chunks per file as a dependent index; fixed interleaved step order with a cross-slot refusal; coordinates never consulted): a segment table on the distributed plan's `Chunk` field, a fourth `group_keys` case with no CSR, the outer axis of a file-segmented dimension as an `EnumIdx` over file labels, `ungroup` as the identity-restoring inverse (today's `join` mints a look-alike axis), per-segment lazy reads with `haloDemand` ghost strips, licensed per-segment folds, coordinate disjointness as a runtime obligation never a metadata-time read, and a multi-dim section (commuting sequence of single-slot segmentations vs genuinely hierarchical grouping) |
| [plan-distributed-memory.md](plan-distributed-memory.md) | DESIGN: nothing built | Chunk-wise distributed memory as a storage level: `Chunked<I, K, ranks>` (a `Chunk` FIELD on the index record, not a kind or tag — `Dist<>` is taken by ppl, `blocked<I,K>` is dead surface and dies), ownership derived from iteration and refused when non-local, elementwise = zero communication by construction, licensed folds via Allgather-of-partials (bit-reproducible at fixed P, which `MPI_Allreduce` cannot claim), `halo` offsets as derived ghost exchange with the missing serial boundary policies landing first; today's replicate-everything `--mpi` coexists then deprecates; P0 gate is provider I/O (per-chunk memcpy), NOT compute — the S0 brick bet already lost that race |
| [plan-fortran-killer.md](plan-fortran-killer.md) | ARC 1 + `where repro` + 6 bugfixes LANDED (feat/expr-fusion); arc 4 → plan-distributed-memory.md; arc 2 deferred; arcs 3/5/6 open | Four-agent audit of the non-symmetry path to displacing Fortran: five arcs (expression fusion, `while`-guarded `let rec` convergence, `foreign` FFI + `--lib`, `DistIdx` distributed memory — today's `--mpi` replicates all storage, CF/compressed data reality), an AD-through-recurrences moonshot, and a 25-entry verified bug census headlined by two silent-OOB soundness holes and units silently disabling BLAS routing |
| [plan-icechunk-provider.md](plan-icechunk-provider.md) | P0–P3 + demo notebook LANDED, hardened after an adversarial review; P4 open | Icechunk (versioned Zarr) as a fourth provider: `repo.checkout("ref"[, ic.tag])` factory dispatching on ref-unit markers, desugared to a canonical path key in one raw-AST pass; all ref→snapshot→chunk resolution at compile time (baked chunk tables, runtime stays pure C++17); axis identity — dense axes and packed pools alike — is shared across checkouts iff extent and content fingerprint are unchanged, survives type aliases, and names the axis and its split reason when it refuses. FlatBuffers/zstd DECIDED (vendored `flatc` accessors + ZstdSharp.Port); resolution failures surface at `blade check` as BL2008; open: P4 packed/orbit/window parity |

Conventions: status changes edit the doc's header and this table — never move or
rename the file. Docs for landed work may be deleted in bulk sweeps, but sweep
their citations in the same change (the ef94c50 deletion did not, and code
comments still cite several dead `docs/plan-*.md` paths).
